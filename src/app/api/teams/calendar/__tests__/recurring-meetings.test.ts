/**
 * Main test suite for recurring meetings route
 * Tests are split into post.test.ts and get.test.ts for better organization
 */

// Import mocks first to ensure they're set up
import "./recurring-meetings/mocks";

// Import test suites
import "./recurring-meetings/post.test";
import "./recurring-meetings/get.test";
